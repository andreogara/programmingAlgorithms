function findMaxWindow(nums, k){
    if(nums.length <= k ){
        return nums;
    }
    var len = nums.length;
    var window = [];
    var queue = [];
    for (var i = 0; i < len; i++){
        if(queue && queue[0] <= i - k){

        }
    }
}